<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<meta name="theme-color" content="#155eef">
<title>Facebook Creator Study</title>

<style>
*{box-sizing:border-box}

body{
margin:0;
font-family:Arial,sans-serif;
background:#f5f7fb;
color:#111827
}

header{
background:linear-gradient(135deg,#155eef,#7c3aed);
color:white;
padding:18px 16px;
position:sticky;
top:0;
z-index:20;
box-shadow:0 3px 12px #0002
}

.head{
max-width:900px;
margin:auto
}

.top{
display:flex;
justify-content:space-between;
align-items:center;
gap:10px
}

.logo{
font-size:20px;
font-weight:800
}

.sub{
font-size:12px;
opacity:.9;
margin-top:3px
}

.logout{
border:0;
background:#ffffff22;
color:white;
padding:9px 12px;
border-radius:10px;
font-weight:bold;
cursor:pointer
}

main{
max-width:900px;
margin:auto;
padding:16px
}

.hero{
background:white;
border-radius:18px;
padding:18px;
box-shadow:0 4px 18px #0000000b;
margin-bottom:16px
}

.hero h1{
margin:0 0 6px;
font-size:24px
}

.hero p{
margin:0;
color:#64748b
}

.langs{
display:flex;
gap:8px;
margin-top:15px;
flex-wrap:wrap
}

.lang{
border:1px solid #dbe3ef;
background:#fff;
padding:9px 13px;
border-radius:10px;
cursor:pointer;
font-weight:bold
}

.lang.active{
background:#155eef;
color:#fff;
border-color:#155eef
}

.status{
margin-top:15px;
padding:13px;
border-radius:13px;
font-weight:bold
}

.status.free{
background:#eff6ff;
color:#155eef
}

.status.premium{
background:#ecfdf5;
color:#047857
}

.status.expired{
background:#fff7ed;
color:#c2410c
}

.progressbox{
background:white;
border-radius:18px;
padding:16px;
margin-bottom:16px;
box-shadow:0 4px 18px #0000000b
}

.progressline{
height:10px;
background:#e5e7eb;
border-radius:20px;
overflow:hidden;
margin:10px 0
}

.progressbar{
height:100%;
width:0%;
background:linear-gradient(90deg,#155eef,#7c3aed);
transition:.3s
}

.progresstext{
font-size:14px;
color:#64748b
}

.premium{
background:linear-gradient(135deg,#111827,#312e81);
color:white;
border-radius:18px;
padding:20px;
margin-bottom:18px;
box-shadow:0 6px 20px #0002
}

.premium h2{
margin:0 0 7px;
font-size:21px
}

.premium p{
margin:5px 0;
opacity:.9
}

.buy{
border:0;
background:white;
color:#312e81;
font-weight:800;
padding:12px 18px;
border-radius:11px;
margin-top:12px;
cursor:pointer
}

.section{
display:flex;
justify-content:space-between;
align-items:center;
margin:18px 2px 10px
}

.section h2{
font-size:19px;
margin:0
}

.count{
font-size:13px;
color:#64748b
}

.lessons{
display:grid;
gap:10px
}

.lesson{
background:white;
border-radius:15px;
padding:15px;
display:flex;
align-items:center;
gap:13px;
box-shadow:0 3px 12px #0000000a;
border:1px solid #eef2f7;
cursor:pointer;
transition:.15s
}

.lesson:active{
transform:scale(.99)
}

.num{
width:43px;
height:43px;
min-width:43px;
border-radius:12px;
background:#eef4ff;
color:#155eef;
display:flex;
align-items:center;
justify-content:center;
font-weight:800;
font-size:14px
}

.lessoninfo{
flex:1;
min-width:0
}

.title{
font-weight:700;
line-height:1.35
}

.meta{
font-size:12px;
color:#94a3b8;
margin-top:4px
}

.lock{
font-size:20px
}

.freebadge{
font-size:10px;
font-weight:800;
background:#dcfce7;
color:#15803d;
padding:5px 7px;
border-radius:7px
}

.premiumbadge{
font-size:10px;
font-weight:800;
background:#ede9fe;
color:#6d28d9;
padding:5px 7px;
border-radius:7px
}

.empty{
background:white;
padding:30px;
text-align:center;
border-radius:15px;
color:#64748b
}

.loading{
text-align:center;
padding:30px;
color:#64748b
}

footer{
text-align:center;
padding:30px 15px;
color:#94a3b8;
font-size:12px
}

@media(min-width:650px){
.lessons{
grid-template-columns:1fr 1fr
}
}
</style>
</head>

<body>

<header>
<div class="head">
<div class="top">
<div>
<div class="logo">📱 Facebook Creator Study</div>
<div class="sub">Learn. Create. Grow.</div>
</div>
<button class="logout" onclick="logout()">Logout</button>
</div>
</div>
</header>

<main>

<section class="hero">
<h1>📚 Creator Study</h1>
<p>Learn practical Facebook creator skills step by step.</p>

<div class="langs">
<button class="lang active" id="enBtn" onclick="setLanguage('en')">🇬🇧 English</button>
<button class="lang" id="taBtn" onclick="setLanguage('ta')">🇱🇰 தமிழ்</button>
<button class="lang" id="siBtn" onclick="setLanguage('si')">🇱🇰 සිංහල</button>
</div>

<div id="status" class="status free">
Checking Premium status...
</div>
</section>

<section class="progressbox">
<strong>📈 Your Progress</strong>
<div class="progressline">
<div class="progressbar" id="progressBar"></div>
</div>
<div class="progresstext" id="progressText">Loading progress...</div>
</section>

<section class="premium" id="premiumBox">
<h2>👑 Premium Access</h2>
<p>Unlock the complete Creator Study library.</p>
<p><strong>LKR 999 / 30 days</strong></p>
<button class="buy" onclick="buyPremium()">Get Premium</button>
</section>

<div class="section">
<h2>📚 Lessons</h2>
<div class="count" id="lessonCount">Loading...</div>
</div>

<div id="lessons" class="lessons">
<div class="loading">Loading lessons...</div>
</div>

</main>

<footer>
Facebook Creator Study<br>
Independent online education platform
</footer>

<script>
const API="https://lesson.createlk.workers.dev";
const PAY="https://createlk-payhere.createlk.workers.dev";
const AUTH="https://createlk-auth-api.createlk.workers.dev";

let token=localStorage.getItem("createlk_token")||"";
let customer=null;
let lessons=[];
let premium=false;
let language=localStorage.getItem("fb_language")||"en";
let completed=new Set();

try{
customer=JSON.parse(localStorage.getItem("createlk_customer")||"null");
}catch(e){
customer=null;
}

function authHeaders(){
return token?{
"Authorization":"Bearer "+token,
"Content-Type":"application/json"
}:{
"Content-Type":"application/json"
};
}

function setLanguage(lang){
language=lang;
localStorage.setItem("fb_language",lang);

document.querySelectorAll(".lang").forEach(x=>x.classList.remove("active"));
document.getElementById(lang+"Btn").classList.add("active");

loadLessons();
}

function updateLanguageButtons(){
document.querySelectorAll(".lang").forEach(x=>x.classList.remove("active"));
const b=document.getElementById(language+"Btn");
if(b)b.classList.add("active");
}

async function checkPremium(){
if(!token){
premium=false;
showStatus();
return;
}

try{
const r=await fetch(
API+"/premium/status?course=facebook-creator",
{
headers:authHeaders()
}
);

if(!r.ok){
premium=false;
showStatus();
return;
}

const d=await r.json();

premium=!!(
d.active===true ||
d.premium===true ||
d.is_premium===true ||
d.status==="ACTIVE" ||
d.membership_status==="PREMIUM"
);

if(d.expires_at){
const expiry=new Date(d.expires_at).getTime();
if(!isNaN(expiry) && expiry<=Date.now()){
premium=false;
}
}

showStatus();

}catch(e){
premium=false;
showStatus();
}
}

function showStatus(){
const box=document.getElementById("status");
const premiumBox=document.getElementById("premiumBox");

if(premium){
box.className="status premium";
box.textContent="✓ Premium Active — All lessons unlocked";
premiumBox.style.display="none";
}else if(token){
box.className="status expired";
box.textContent="Premium not active — Lessons 1–10 are free";
premiumBox.style.display="block";
}else{
box.className="status free";
box.textContent="Free access — Lessons 1–10 available";
premiumBox.style.display="block";
}
}

async function loadLessons(){
const container=document.getElementById("lessons");
container.innerHTML='<div class="loading">Loading lessons...</div>';

try{
const r=await fetch(
API+"/lessons?course=facebook-creator&language="+encodeURIComponent(language)
);

if(!r.ok)throw new Error("Lessons failed");

const d=await r.json();

lessons=
Array.isArray(d)?
d:
(d.lessons||d.data||[]);

lessons.sort((a,b)=>
Number(a.lesson_number||a.number||0)-
Number(b.lesson_number||b.number||0)
);

renderLessons();

}catch(e){
container.innerHTML=
'<div class="empty">Unable to load lessons. Please refresh.</div>';
document.getElementById("lessonCount").textContent="";
}
}

function renderLessons(){

const container=document.getElementById("lessons");

if(!lessons.length){
container.innerHTML='<div class="empty">No lessons available.</div>';
return;
}

document.getElementById("lessonCount").textContent=
lessons.length+" lessons";

container.innerHTML="";

lessons.forEach((lesson,index)=>{

const number=Number(
lesson.lesson_number||
lesson.number||
lesson.lesson||
index+1
);

const isFree=
lesson.is_free===true||
lesson.is_free===1||
number<=10;

const unlocked=isFree||premium;

const done=
completed.has(number)||
lesson.completed===true;

const title=
lesson.title||
lesson.title_en||
lesson.name||
("Lesson "+number);

const div=document.createElement("div");
div.className="lesson";

div.onclick=()=>{
openLesson(number,unlocked);
};

div.innerHTML=`
<div class="num">${number}</div>

<div class="lessoninfo">
<div class="title">${escapeHtml(title)}</div>
<div class="meta">
${done?"✓ Completed • ":""}
${isFree?"Free":"Premium"}
</div>
</div>

<div>
${
done
?'<span class="freebadge">✓</span>'
:unlocked
?'<span class="freebadge">OPEN</span>'
:'<span class="lock">🔒</span>'
}
</div>
`;

container.appendChild(div);
});
}

function openLesson(number,unlocked){

if(!unlocked){
window.location.href="/fb/premium.html";
return;
}

window.location.href=
"/fb/lesson.html?lesson="+encodeURIComponent(number)+
"&language="+encodeURIComponent(language);
}

async function loadProgress(){

if(!token){
document.getElementById("progressText").textContent=
"Login to track your progress.";
return;
}

try{
const r=await fetch(
API+"/progress?course=facebook-creator",
{
headers:authHeaders()
}
);

if(!r.ok)throw new Error();

const d=await r.json();

let list=
d.completed||
d.lessons||
d.progress||
d.data||
[];

if(!Array.isArray(list)){
if(typeof list==="object"){
list=Object.keys(list).filter(k=>list[k]);
}else{
list=[];
}
}

completed=new Set(
list.map(x=>{
if(typeof x==="number")return x;
if(typeof x==="string")return Number(x);
return Number(
x.lesson_number||
x.lesson||
x.lesson_id||
x.number
);
}).filter(x=>x>0)
);

const total=lessons.length;
const done=completed.size;
const percent=total?Math.round(done/total*100):0;

document.getElementById("progressBar").style.width=
percent+"%";

document.getElementById("progressText").textContent=
done+" of "+total+" completed • "+percent+"%";

renderLessons();

}catch(e){
document.getElementById("progressText").textContent=
"Progress unavailable";
}
}

async function buyPremium(){

if(!token){
window.location.href="/fb/login.html";
return;
}

const button=document.querySelector(".buy");
button.disabled=true;
button.textContent="Preparing payment...";

try{

const r=await fetch(
PAY+"/premium-checkout",
{
method:"POST",
headers:{
"Authorization":"Bearer "+token,
"Content-Type":"application/json"
},
body:JSON.stringify({
course:"facebook-creator"
})
}
);

const d=await r.json();

if(!r.ok){
throw new Error(d.error||"Checkout failed");
}

const fields=d.fields||d.checkout;

if(!fields||typeof fields!=="object"){
throw new Error("Invalid payment response");
}

const form=document.createElement("form");
form.method="POST";
form.action="https://www.payhere.lk/pay/checkout";
form.style.display="none";

Object.entries(fields).forEach(([key,value])=>{
const input=document.createElement("input");
input.type="hidden";
input.name=key;
input.value=value==null?"":value;
form.appendChild(input);
});

document.body.appendChild(form);
form.submit();

}catch(e){

alert(e.message||"Payment could not be started.");

button.disabled=false;
button.textContent="Get Premium";
}
}

function logout(){

localStorage.removeItem("createlk_token");
localStorage.removeItem("createlk_customer");

window.location.href="/fb/login.html";
}

function escapeHtml(value){

return String(value)
.replace(/&/g,"&amp;")
.replace(/</g,"&lt;")
.replace(/>/g,"&gt;")
.replace(/"/g,"&quot;")
.replace(/'/g,"&#039;");
}

async function init(){

if(!token){
showStatus();
}else{
await checkPremium();
}

updateLanguageButtons();

await loadLessons();

await loadProgress();
}

init();
</script>

</body>
</html>