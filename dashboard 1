<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>CreateLK Academy | Student Dashboard</title>
<meta name="theme-color" content="#155eef">

<style>
*{box-sizing:border-box;margin:0;padding:0}
:root{
--p:#155eef;--v:#7c3aed;--g:#12b76a;
--o:#d97706;--t:#172033;--m:#667085;
--b:#e4e7ec;--bg:#f5f7fb
}
body{
font-family:Arial,Helvetica,sans-serif;
background:var(--bg);color:var(--t);
font-size:14px;line-height:1.5
}
a{text-decoration:none}
button{font-family:inherit}

header{
position:sticky;top:0;z-index:10;
background:#fff;border-bottom:1px solid var(--b)
}
.nav,.container{
max-width:1050px;margin:auto
}
.nav{
padding:12px 16px;display:flex;
justify-content:space-between;align-items:center
}
.logo{
font-size:18px;font-weight:900;color:var(--p)
}
.logout{
border:1px solid var(--b);background:#fff;
padding:8px 12px;border-radius:8px;
font-size:11px;font-weight:800;cursor:pointer
}
.container{padding:18px 16px 35px}

.welcome{
display:flex;justify-content:space-between;
align-items:flex-end;gap:12px;margin-bottom:14px
}
.welcome h1{
font-size:26px;line-height:1.2
}
.welcome-sub{
color:var(--m);font-size:12px
}
.student-id{
display:inline-block;margin-top:6px;
padding:5px 9px;border-radius:8px;
background:#eaf1ff;border:1px solid #c7d7fe;
color:var(--p);font-size:10px;font-weight:800
}
.learning{
padding:7px 10px;border-radius:20px;
background:#ecfdf3;border:1px solid #abefc6;
color:#067647;font-size:10px;font-weight:800
}
.dot{
display:inline-block;width:7px;height:7px;
border-radius:50%;background:var(--g);
margin-right:5px
}

.card{
background:#fff;border:1px solid var(--b);
border-radius:15px;padding:16px;
box-shadow:0 3px 15px rgba(16,24,40,.04)
}

.teacher{
display:flex;align-items:center;gap:16px;
background:linear-gradient(135deg,#eef4ff,#f8f5ff);
border-color:#c7d7fe;margin-bottom:14px
}
.teacher img{
width:145px;height:110px;object-fit:cover;
border-radius:12px;flex-shrink:0
}
.label{
font-size:10px;font-weight:900;
color:var(--v);text-transform:uppercase
}
.teacher h2{
font-size:22px;line-height:1.2;margin:3px 0
}
.teacher p{
font-size:12px;color:#475467
}
.btn{
display:inline-block;margin-top:9px;
padding:9px 14px;border-radius:9px;
background:linear-gradient(135deg,var(--p),var(--v));
color:#fff;font-size:11px;font-weight:800
}

.grid{
display:grid;
grid-template-columns:1fr 1fr;
gap:14px
}
.title{
display:flex;align-items:center;gap:8px;
margin-bottom:10px
}
.title h2{font-size:16px}
.icon{
width:34px;height:34px;border-radius:10px;
display:grid;place-items:center;
background:#eef2f7
}

.practice{
background:linear-gradient(135deg,#eef4ff,#f5f3ff);
border-color:#c7d7fe
}
.badge{
float:right;padding:5px 9px;
border-radius:20px;background:#e0ecff;
color:var(--p);font-size:9px;font-weight:900
}
.practice-number{
font-size:32px;font-weight:900;color:var(--p)
}
.muted{
font-size:11px;color:var(--m)
}
.bar{
height:8px;background:#dbe5ff;
border-radius:20px;overflow:hidden;margin-top:10px
}
.bar div{
height:100%;width:0;
background:linear-gradient(90deg,var(--p),var(--v));
transition:.4s
}
.practice-info{
display:flex;justify-content:space-between;
font-size:10px;color:var(--m);margin-top:5px
}
.full-btn{
display:block;text-align:center;
margin-top:12px;padding:10px;border-radius:9px;
background:linear-gradient(135deg,var(--p),var(--v));
color:#fff;font-size:11px;font-weight:800
}

.stats{
display:grid;
grid-template-columns:repeat(4,1fr);
gap:6px
}
.stat{
text-align:center;background:#fff;
border:1px solid var(--b);
border-radius:10px;padding:10px 3px
}
.stat strong{
display:block;font-size:18px;color:var(--p)
}
.stat span{
font-size:9px;color:var(--m)
}

.coach{
background:linear-gradient(135deg,#f5f3ff,#faf5ff);
border-color:#ddd6fe
}
.message{
padding:12px;background:#fff;
border:1px solid #e5e7eb;
border-radius:10px;
font-size:12px;color:#475467
}

.certificate{
background:linear-gradient(135deg,#fffbeb,#fff7ed);
border-color:#f3dfb0
}
.cert-number{
font-size:28px;font-weight:900;color:#b45309
}
.cert-bar{
height:9px;background:#eadfc8;
border-radius:20px;overflow:hidden;margin-top:9px
}
.cert-bar div{
height:100%;width:0;
background:linear-gradient(90deg,#f59e0b,#d97706)
}
.cert-info{
display:flex;justify-content:space-between;
font-size:10px;color:var(--m);margin-top:5px
}
.status{
margin-top:9px;padding:7px;border-radius:20px;
text-align:center;font-size:10px;font-weight:800;
background:#fef3c7;color:#92400e
}
.locked,.unlocked{
display:block;text-align:center;
margin-top:9px;padding:10px;border-radius:9px;
font-size:11px;font-weight:800
}
.locked{
background:#d0d5dd;color:#667085
}
.unlocked{
background:#f59e0b;color:#fff
}

.premium{
grid-column:1/-1;
background:linear-gradient(135deg,#172554,#155eef,#4f46e5);
color:#fff;border:0
}
.premium-top{
display:flex;justify-content:space-between;
gap:15px
}
.premium h2{font-size:20px}
.premium p{
font-size:12px;opacity:.9
}
.price{
font-size:22px;font-weight:900;
white-space:nowrap
}
.highlight{
margin:13px 0;padding:11px;
background:#ffffff12;
border:1px solid #ffffff20;
border-radius:10px
}
.highlight strong{font-size:25px}
.features{
display:grid;
grid-template-columns:repeat(3,1fr);
gap:6px;margin:10px 0
}
.features span{
padding:7px;
background:#ffffff10;
border:1px solid #ffffff18;
border-radius:7px;
font-size:10px
}
.premium-btn,.active{
display:block;width:100%;padding:11px;
border-radius:9px;text-align:center;
font-size:11px;font-weight:800
}
.premium-btn{
border:0;background:#fff;color:var(--p);
cursor:pointer
}
.premium-btn:disabled{opacity:.7}
.active{
background:#dcfce7;
color:#166534
}
.countdown{
margin-top:8px;padding:8px;
background:#ffffff12;
border:1px solid #ffffff20;
border-radius:8px;
text-align:center;font-size:11px
}
.countdown small{
display:block;font-size:9px;opacity:.7
}

#loading{
text-align:center;color:var(--m);padding:15px
}
footer{
text-align:center;padding:20px;
color:#98a2b3;font-size:10px
}

@media(max-width:750px){
.grid{grid-template-columns:1fr}
.premium{grid-column:auto}
.teacher img{
width:120px;height:100px
}
.features{
grid-template-columns:1fr 1fr
}
}

@media(max-width:550px){
.container{
padding:14px 12px 30px
}
.welcome{
align-items:flex-start;
flex-direction:column
}
.welcome h1{font-size:23px}
.teacher{gap:10px}
.teacher img{
width:100px;height:90px
}
.teacher h2{font-size:18px}
.teacher p{font-size:11px}
.card{padding:14px}
.premium-top{gap:8px}
.premium h2{font-size:18px}
.price{font-size:18px}
}

@media(max-width:380px){
.teacher img{
width:85px;height:80px
}
.teacher h2{font-size:16px}
.teacher p{font-size:10px}
.stats{gap:4px}
.stat strong{font-size:16px}
.features span{font-size:8px}
}
</style>
</head>

<body>

<header>
<div class="nav">
<div class="logo">📚 CreateLK Academy</div>
<button class="logout" onclick="logout()">Logout</button>
</div>
</header>

<main class="container">

<section class="welcome">
<div>
<h1>👋 Welcome, <span id="firstName">Student</span></h1>
<p class="welcome-sub">
Continue your English learning journey.
</p>
<div class="student-id" id="studentId">
Student ID: —
</div>
</div>

<div class="learning">
<span class="dot"></span>Learning Active
</div>
</section>

<section class="card teacher">

<img
src="https://assets.createlk.com/file_0000000018e08208b00cf7445bca7027.png"
alt="CreateLK AI English Teacher"
loading="lazy"
>

<div>
<div class="label">🤖 AI English Teacher</div>
<h2>Your Personal AI Teacher</h2>
<p>
Practise grammar, vocabulary and everyday English with AI-powered practice.
</p>
<a href="ai-mcq.html" class="btn">
Start Today's Practice →
</a>
</div>

</section>

<div id="loading" class="card">
Loading your dashboard...
</div>

<div id="content" style="display:none">

<div class="grid">

<section class="card practice">

<span id="practiceBadge" class="badge">FREE</span>

<div class="title">
<div class="icon">📝</div>
<h2>Today's Practice</h2>
</div>

<p class="muted">
Complete your daily AI English practice.
</p>

<div style="margin-top:10px">
<span id="used" class="practice-number">0</span>
<span class="muted">
 / <span id="limit">10</span> questions
</span>
</div>

<div class="bar">
<div id="practiceBar"></div>
</div>

<div class="practice-info">
<span>Daily progress</span>
<span>
<b id="remaining">10</b> remaining
</span>
</div>

<a href="ai-mcq.html" class="full-btn">
Start Practice →
</a>

</section>

<section class="card">

<div class="title">
<div class="icon">📊</div>
<h2>Your Progress</h2>
</div>

<div class="stats">

<div class="stat">
<strong id="accuracy">0%</strong>
<span>Accuracy</span>
</div>

<div class="stat">
<strong id="total">0</strong>
<span>Questions</span>
</div>

<div class="stat">
<strong id="correct">0</strong>
<span>Correct</span>
</div>

<div class="stat">
<strong id="streak">0</strong>
<span>Streak</span>
</div>

</div>

</section>

<section class="card coach">

<div class="title">
<div class="icon">💡</div>
<h2>AI Coach</h2>
</div>

<div class="message" id="coach">
Start practising to receive personalised guidance from your AI Coach.
</div>

</section>

<section class="card certificate">

<div class="title">
<div class="icon">🏆</div>
<h2>Certificate Progress</h2>
</div>

<p class="muted">
Reach 1,000 correct answers to unlock your certificate.
</p>

<div style="margin-top:6px">
<span id="certificateProgress" class="cert-number">0</span>
<span class="muted"> / 1,000</span>
</div>

<div class="cert-bar">
<div id="certificateBar"></div>
</div>

<div class="cert-info">
<span>Progress</span>
<span id="certificatePercent">0%</span>
</div>

<div id="certificateStatus" class="status">
1,000 correct answers required
</div>

<a id="certificateButton" class="locked">
Certificate Locked
</a>

</section>

<section class="card premium">

<div class="premium-top">

<div>
<h2 id="membershipTitle">
⭐ Unlock Premium
</h2>

<p id="membershipText">
Get 100 AI English questions every day for 30 days.
</p>
</div>

<div class="price">
LKR 1,500
</div>

</div>

<div class="highlight">
<strong>100</strong> AI questions every day<br>
<small>10× more daily practice than Free</small>
</div>

<div class="features">
<span>✓ 100 AI MCQs/day</span>
<span>✓ Advanced Progress</span>
<span>✓ Grammar</span>
<span>✓ Vocabulary</span>
<span>✓ Certificate</span>
<span>✓ 30 Days</span>
</div>

<button
id="premiumButton"
class="premium-btn"
onclick="startPayment()">
⭐ Upgrade to Premium — LKR 1,500
</button>

<div
id="premiumActive"
class="active"
style="display:none">
✓ Premium Active
</div>

<div
id="countdown"
class="countdown"
style="display:none">

<small>Premium remaining</small>

<span id="countdownText"></span>

</div>

</section>

</div>
</div>

</main>

<footer>
© 2026 CreateLK Academy. All rights reserved.
</footer>

<script>

const API=
"https://createlk-ai-mcq.createlk.workers.dev";

const CHECKOUT=
"https://createlk-payhere-checkout.createlk.workers.dev";

const KEY="createlk_student";

const REQUIRED=1000;

let student=null;
let countdownTimer=null;


/* SESSION */

try{

student=JSON.parse(
sessionStorage.getItem(KEY)||"null"
);

}catch(e){

student=null;

}

if(!student){

location.replace("login.html");

}

const id=
student?.id||
student?.student_id||
student?.user_id;

if(!id){

location.replace("login.html");

}

const name=
student?.name||
`${student?.first_name||""} ${student?.last_name||""}`.trim()||
"Student";

document.getElementById("firstName").textContent=
name.split(" ")[0];

document.getElementById("studentId").textContent=
"Student ID: "+id;


/* LOGOUT */

function logout(){

sessionStorage.removeItem(KEY);
sessionStorage.removeItem("createlk_logged_in");

location.replace("login.html");

}


/* NUMBER */

function number(v){

const n=Number(v);

return Number.isFinite(n)?n:0;

}


/* VALUE */

function value(data,keys){

const sources=[
data,
data?.progress,
data?.stats,
data?.statistics
];

for(const source of sources){

if(!source)continue;

for(const key of keys){

if(
source[key]!==undefined&&
source[key]!==null&&
source[key]!==""
){

return number(source[key]);

}

}

}

return 0;

}


/* PREMIUM */

function setPremium(data){

const status=
String(data?.membership_status||"").toUpperCase();

const plan=
String(data?.plan||"").toUpperCase();

const active=
data?.premium===true||
status==="ACTIVE"||
status==="PREMIUM"||
plan==="PREMIUM";

const badge=
document.getElementById("practiceBadge");

badge.textContent=
active?"PREMIUM":"FREE";

if(active){

badge.style.background="#ede9fe";
badge.style.color="#6d28d9";

}else{

badge.style.background="#e0ecff";
badge.style.color="#155eef";

}

document.getElementById("membershipTitle").textContent=
active
?"⭐ Premium Membership Active"
:"⭐ Unlock Premium";

document.getElementById("membershipText").textContent=
active
?"You have access to 100 AI questions every day."
:"Get 100 AI English questions every day for 30 days.";

document.getElementById("premiumButton").style.display=
active?"none":"block";

document.getElementById("premiumActive").style.display=
active?"block":"none";

if(active){

const expiry=
data?.membership_expiry||
data?.premium_expiry||
data?.expiry||
student?.membership_expiry;

if(expiry){

document.getElementById("countdown").style.display=
"block";

startCountdown(expiry);

}

}

return active;

}


/* COUNTDOWN */

function startCountdown(expiry){

const date=new Date(expiry);

if(isNaN(date.getTime()))return;

clearInterval(countdownTimer);

function update(){

const ms=date-Date.now();

if(ms<=0){

clearInterval(countdownTimer);

document.getElementById("countdownText").textContent=
"Premium expired";

return;

}

const seconds=Math.floor(ms/1000);

const days=Math.floor(seconds/86400);

const hours=
Math.floor((seconds%86400)/3600);

const minutes=
Math.floor((seconds%3600)/60);

const sec=seconds%60;

document.getElementById("countdownText").textContent=
`${days}d ${hours}h ${minutes}m ${sec}s`;

}

update();

countdownTimer=setInterval(update,1000);

}


/* CERTIFICATE */

function certificate(correct){

const percent=
Math.min(correct/REQUIRED*100,100);

document.getElementById("certificateProgress").textContent=
correct.toLocaleString();

document.getElementById("certificateBar").style.width=
percent+"%";

document.getElementById("certificatePercent").textContent=
Math.floor(percent)+"%";

const status=
document.getElementById("certificateStatus");

const button=
document.getElementById("certificateButton");

if(correct>=REQUIRED){

status.textContent=
"🎉 Certificate Unlocked";

status.style.background="#dcfce7";
status.style.color="#166534";

button.textContent=
"Open Certificate →";

button.href="certificates.html";
button.className="unlocked";

}else{

const left=
REQUIRED-correct;

status.textContent=
left.toLocaleString()+
" correct answers remaining";

button.textContent=
"Certificate Locked";

button.removeAttribute("href");
button.className="locked";

}

}


/* PAYMENT */

async function startPayment(){

if(!student?.email){

alert(
"Your account email is missing. Please log in again."
);

return;

}

const button=
document.getElementById("premiumButton");

button.disabled=true;

button.textContent=
"Connecting to PayHere...";

try{

const response=await fetch(
CHECKOUT,
{
method:"POST",

headers:{
"Content-Type":"application/json"
},

body:JSON.stringify({

student_id:String(id),

first_name:
student.first_name||
name.split(" ")[0],

last_name:
student.last_name||"",

email:student.email,

phone:student.phone||""

})

}
);

const data=await response.json();

if(
!response.ok||
data.success!==true
){

throw new Error(
data.message||
"Unable to create payment."
);

}

const form=
document.createElement("form");

form.method="POST";

form.action=
data.action||
"https://www.payhere.lk/pay/checkout";

Object.entries(
data.payment||{}
).forEach(([key,val])=>{

const input=
document.createElement("input");

input.type="hidden";
input.name=key;
input.value=val??"";

form.appendChild(input);

});

document.body.appendChild(form);

form.submit();

}catch(error){

alert(
"Unable to start payment.\n\n"+
error.message
);

button.disabled=false;

button.textContent=
"⭐ Upgrade to Premium — LKR 1,500";

}

}


/* LOAD DASHBOARD */

async function loadDashboard(){

try{

const response=await fetch(
`${API}/api/progress?student_id=${encodeURIComponent(id)}`,
{
cache:"no-store",

headers:{
Accept:"application/json"
}

}
);

const data=await response.json();

if(
!response.ok||
data.success!==true
){

throw new Error(
data.message||
"Unable to load progress."
);

}


/* PREMIUM */

const premium=setPremium(data);


/*
 DAILY LIMIT
 FREE = 10
 PREMIUM = 100

 Uses the same clamping logic as
 the AI MCQ page so the dashboard
 never displays 11 / 10.
*/

const defaultLimit=
premium?100:10;

let limit=number(
data.daily_limit??
data.limit
);

let used=number(
data.daily_used??
data.used??
data.today_used
);

let remaining=number(
data.remaining??
data.remaining_today
);

if(!limit||limit<1){

limit=defaultLimit;

}

/*
 If the API provides remaining but not
 a valid used value, calculate used.
*/

if(
(!Number.isFinite(Number(data.daily_used)) &&
 !Number.isFinite(Number(data.used)) &&
 !Number.isFinite(Number(data.today_used))) &&
Number.isFinite(remaining)
){

used=limit-remaining;

}

/*
 If remaining is not supplied, calculate it.
*/

if(
!Number.isFinite(Number(data.remaining)) &&
!Number.isFinite(Number(data.remaining_today))
){

remaining=limit-used;

}

/*
 Never allow the dashboard to display
 usage above the user's daily limit.
*/

used=Math.max(
0,
Math.min(limit,used)
);

remaining=Math.max(
0,
Math.min(limit,remaining)
);

/*
 Keep remaining mathematically consistent
 with the displayed used value.
*/

remaining=Math.max(
0,
limit-used
);

document.getElementById("used").textContent=
used;

document.getElementById("limit").textContent=
limit;

document.getElementById("remaining").textContent=
remaining;

const dailyPercent=
Math.min(
used/limit*100,
100
);

document.getElementById("practiceBar").style.width=
dailyPercent+"%";


/* PROGRESS */

const correct=value(data,[
"correct_answers",
"correct_count",
"total_correct",
"correct",
"correctAnswers"
]);

const total=value(data,[
"total_questions",
"questions",
"question_count",
"total"
]);

let accuracy=value(data,[
"accuracy",
"accuracy_percent",
"percentage"
]);

if(!accuracy&&total){

accuracy=
correct/total*100;

}

document.getElementById("accuracy").textContent=
Math.round(accuracy)+"%";

document.getElementById("total").textContent=
total.toLocaleString();

document.getElementById("correct").textContent=
correct.toLocaleString();

document.getElementById("streak").textContent=
number(
data.day_streak??
data.streak
);


/* CERTIFICATE */

certificate(correct);


/* AI COACH */

const coach=
document.getElementById("coach");

if(total===0){

coach.textContent=
"Start practising today and your AI Coach will guide your progress.";

}else if(accuracy>=80){

coach.textContent=
"Excellent work! Your English progress is looking strong. Keep going!";

}else if(accuracy>=60){

coach.textContent=
"Good progress! Continue practising every day to improve your accuracy.";

}else{

coach.textContent=
"Keep practising. Review your mistakes and try to improve your accuracy.";

}

}catch(error){

console.error(
"Dashboard:",
error
);

document.getElementById("coach").textContent=
"Your progress could not be loaded. Please refresh the page.";

}finally{

document.getElementById("loading").style.display=
"none";

document.getElementById("content").style.display=
"block";

}

}

loadDashboard();

</script>

</body>
</html>